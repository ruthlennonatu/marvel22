class viewSessionSchedule{
    public static void main(String[] args) {
	Scanner keyIn = new Scanner( System.in );
        string sessionName;
        int roomNumber;
	int choice;
	

        //System.out.println("1. Class Timetable");
        //System.out.println("2. Room Timetable");
        //System.out.println("Please choose what timetable you wish to see.");	
        //choice = keyIn.nextInt()
	//if(choice == 1)
	//{
	//	System.out.println("Please enter sessionName: ");
	//	sessionName = sessionNameTextBox.Text;
	// 	try {
        //    String sql="SELECT `id`, `image` FROM `img` WHERE id='"+txtdisplayid.getText()+"'";          
        //   PreparedStatement pst=conn.prepareStatement(sql);
        //   ResultSet rs=pst.executeQuery();
        //   if(rs.next())
        //   {          
        //       byte[] imagedata=rs.getBytes("image");
        //       format=new ImageIcon(imagedata);
        //       Image mm=format.getImage();
        //       Image img2=mm.getScaledInstance(lbldisplay.getWidth(),        lbldisplay.getHeight(), Image.SCALE_SMOOTH);
        //       ImageIcon image=new ImageIcon(img2);
        //      lbldisplay.setIcon(image);
        //   }
	//else if(choice == 2)
	//{
	//	System.out.println("Please enter roomNumber: ");
	//	roomNumber = roomNumberTextBox.Text;
	//	try {
        //    String sql="SELECT `id`, `image` FROM `img` WHERE id='"+txtdisplayid.getText()+"'";          
        //   PreparedStatement pst=conn.prepareStatement(sql);
        //   ResultSet rs=pst.executeQuery();
        //   if(rs.next())
        //   {          
        //       byte[] imagedata=rs.getBytes("image");
        //       format=new ImageIcon(imagedata);
        //       Image mm=format.getImage();
        //       Image img2=mm.getScaledInstance(lbldisplay.getWidth(),        lbldisplay.getHeight(), Image.SCALE_SMOOTH);
        //       ImageIcon image=new ImageIcon(img2);
        //      lbldisplay.setIcon(image);
        //   }
    }
}